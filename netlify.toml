[build]
  command = "echo 'Static site ready'"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["csv-parse", "csv-stringify", "exceljs", "jszip"]
